using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CoMFrameButton : MonoBehaviour
{
    // Start is called before the first frame update
    public void MoveToCoMFrame()
    {
        FollowCoM cam = GetComponent<FollowCoM>();
       
    }
}
